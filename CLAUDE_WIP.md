# Current Work in Progress

## Lazy session creation
**Branch:** `feature/lazy-sessions`

### Tasks
- [x] Move `startSession()` from `SessionFormScreen` mount into `handleLogThrow`, triggered only on first entry
- [x] Update `sessionIdRef` logic to handle the session not existing yet on first log
- [x] Update README to reflect the change

**Ready to squash merge.**
