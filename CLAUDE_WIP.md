# Current Work in Progress

## Enrich the roadmap in CLAUDE_TODO.md

Work through the tasks in CLAUDE_TODO until they are well-defined enough to be marked `ready`. Discuss open questions with Oskar. Once a task reaches `ready`, pick one and move it here as the next active task.

### What "ready" means
- No blocking open questions
- Clear description of the change and where it lives in the codebase
- Edge cases and UX entry points are decided

### Current task statuses
- **Lazy session creation** — ready
- **Editable entries** — idea (open: UI entry point, which form to use for editing)
- **Form export / import** — idea (open: export format, ID conflict handling)
- **Move defaults to assets** — idea (blocked by: form export/import format)
- **Parameter overview screen** — idea (open: navigation entry point, read-only vs editable)
- **Dynamic forms redesign** — idea (needs design conversation before anything else)
